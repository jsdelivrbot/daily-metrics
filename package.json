{
  "name": "daily-records",
  "version": "0.0.1",
  "description": "daily recording of various metrics",
  "main": "index.js",
  "scripts": {
    "dev" : "npm run dev-build && nodemon ./lib/index.js",
    "test": "npm run dev-build && npm run test-build && NODE_ENV=test mocha",
    "test-build": "rm -rf test/* && babel --presets es2015,react test-src -d test",
    "dev-build": "rm -rf lib/* && babel --presets es2015,react src -d lib"
  },
  "author": "Tom Scott",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.1",
    "babel-preset-es2015": "^6.22.0",
    "webpack": "^2.2.1",
    "webpack-dev-server": "^2.3.0"
  },
  "dependencies": {
    "babel-preset-react": "^6.23.0",
    "babel-preset-stage-1": "^6.22.0",
    "body-parser": "^1.16.1",
    "express": "^4.14.1",
    "mocha": "^3.2.0",
    "mongoose": "^4.8.3",
    "nodemon": "^1.11.0",
    "supertest": "^3.0.0"
  }
}
